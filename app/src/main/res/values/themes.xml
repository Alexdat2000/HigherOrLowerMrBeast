<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HigherOrLowerMrBeast" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:fontFamily">Serif</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="Button.Higher" parent="ThemeOverlay.AppCompat">
        <item name="backgroundTint">@color/higher</item>
    </style>

    <style name="Button.Lower" parent="ThemeOverlay.AppCompat">
        <item name="backgroundTint">@color/lower</item>
    </style>
</resources>
